{
  "permissions": {
    "allow": [
      "Bash(git commit -m ':*)",
      "Bash(git commit -m 'Fix Vehicle model: allow empty name for unknown-vehicle prompts:*)",
      "Bash(git commit -m 'Add Paste button for mobile clipboard image upload:*)"
    ]
  }
}
